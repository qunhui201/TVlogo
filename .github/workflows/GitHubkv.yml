name: 自动同步 GitHub 文件到 KV

on:
  schedule:
    - cron: "0 * * * *"  # 每小时触发一次
  workflow_dispatch:  # 手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 触发同步请求
        run: |
          curl -X GET "https://live.yuye.qzz.io/sync"
